// No need for this to be its own helper.
// Not sure the variable names here describe accurately what's happening. Perhaps modal should be 'showModal'?
const ToggleModal = (toggleModal, modal) => {
  toggleModal(!modal);
};
export default ToggleModal;
